# Models

Runtime AI model files are downloaded locally and are not committed to Git.

- Run `download_funasr.bat` to cache SenseVoice/FunASR.
- Run `download_kokoro.bat` to install the local Kokoro TTS model.

Run `python scripts/download_kokoro.py` from the project root.

The downloader installs the official sherpa-onnx Kokoro multilingual model into:

`models/kokoro/kokoro-int8-multi-lang-v1_1/`

The model is not committed to Git because it is hundreds of megabytes.

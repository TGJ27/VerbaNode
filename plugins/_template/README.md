# External plugin template

Copy this entire folder and rename it without the leading underscore. Update the manifest ID and the schema `function.name` to the same lowercase identifier. Folders beginning with `_` are intentionally ignored by discovery.
